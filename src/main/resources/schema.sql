CREATE TABLE IF NOT EXISTS ACCOUNT (
                                       ID BIGINT AUTO_INCREMENT PRIMARY KEY,
                                       NAME VARCHAR(255),
    EMAIL VARCHAR(255),
    BALANCE DOUBLE
    );